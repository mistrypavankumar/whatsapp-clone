import styled from "styled-components";

const Chat = ({ id, user }) => {
  return (
    <Container>
      <p>Some chat</p>
    </Container>
  );
};

export default Chat;

const Container = styled.div``;
